/* there are 32 keywords in c 

Keywords are predefined (reserved) words in C that have special meaning to the compiler.

They are part of the language syntax
They cannot be used as identifiers (variable, function, or struct names)

We cannot use these keywords as identifiers (such as variable names, function names, or struct names). The compiler will throw an error if we try to do so.

Data Type Keywords -

char, int, float, double, void, short, long, signed, unsigned

Operator & Utility Keywords - sizeof, return, goto, typedef

Control Flow Keywords - if, else, switch, case, default, for, while, do, break, continue

Storage Class Keywords - auto, register, static, extern

Type Qualifiers - const, volatile

User Defined Types - struct, union, enum

- auto
- break
- case
- char
- const
- continue
- default
- do
- double
- else
- enum
- extern
- float
- for
- goto
- if
- int
- long
- register
- return
- short
- signed
- sizeof
- static
- struct
- switch
- typedef
- union
- unsigned
- void
- volatile
- while


Keywords
Keywords are specific reserved words in C each of which has a specific feature associated with it. Almost all of the words which help us use the functionality of the C language are included in the list of keywords. So, you can imagine that the list of keywords is not going to be a small one! There are a total of 32 keywords in C:

Specify the type/kind of entity.

Case-sensitive and must be written in lowercase.

Keywords are part of the syntax of C and are used to structure the program.

A keyword should be in lower case and can only contains alphabetical characters.

They help to identify a specific property that exists within a computer language.

Keywords cannot be modified or used for any other purpose.

Examples - int, char, if, while, do, class etc.	

Identifiers
Identifiers are used as the general terminology for naming of variables, functions and arrays. These are user defined names consisting of arbitrarily long sequence of letters and digits with either a letter or the underscore(_) as a first character. Identifier names must differ in spelling and case from any keywords. You cannot use keywords as identifiers; they are reserved for special use. Once declared, you can use the identifier in later program statements to refer to the associated value. A special kind of identifier, called a statement label, can be used in goto statements.

Identify the name of a particular entity.

Case-sensitive, can start with a letter or underscore (_).

Identifiers are used for variable names, function names, and more throughout the code.

An identifier can be in upper case or lower case and can consist of alphabetical characters, digits and underscores.

They help to locate the name of the entity that gets defined along with a keyword.

Identifiers can be created and used as per the programmer's needs.

Examples - Test, count1, high_speed, etc.
*/