// Level 4 Challenge: Functions
// As programs grow, stuffing everything into int main() makes the code messy and hard to maintain. Real production code uses functions to break tasks into reusable pieces.

// Let's isolate our business logic.

// Your Task:
// Create a separate function named convertToEur that handles just the math.

// Function Signature: The function should take a float (the USD amount) as an input parameter and return a float (the final EUR amount).

// Move the Logic: Inside this function, subtract the $2 fee and calculate the conversion.

// Call it in Main: In your main() function, after the loop finishes getting a valid input, call convertToEur(USD) and store the result in your EUR variable.

