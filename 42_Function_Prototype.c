#include<stdio.h>
void data (char name[100] , int age ){
    printf("hello %s  \n",name);
    printf("hello %s  \n",name);
    
}
int main ()
{
    // function prototype : provide the compiler w/ information about the function 
    /*The functions name, return type , parameter before it's actual definition 
    enables the type checking and allow the function to be used before they are defined . this will improve readability , organization , and helps to prevent the errors   */
    return 0 ;
}