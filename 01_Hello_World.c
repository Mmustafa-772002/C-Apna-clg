// This is a simple C program that prints "Hello World" to the console.


#include<stdio.h>
// this is preprocessor for the c program 
int main()

{
    printf("Hello World");
    // this is used to print the the statement on the screen 
    // this is for the display purpose 
    // every stmt which necessary should be closed with ; 

    // for the programmer understanding in the comments are written with the specific line to the next programmer this lines does not affect on the code 
    // this comment is ignored by the compiler 
    // this specific symbol is  '//' for single line comment 
    // for mutiple comment there is another symbol is used '/* comment */' 
    return 0;
    // the main function is expected to return a value is 0 then it successfully compiled the code  
    // it not then it shows the error this number is called that hexadecimal number 
}